version: '3.8'

services:
  web:
    build: .
    # Команда, яка запустить Django-сервер всередині контейнера
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"